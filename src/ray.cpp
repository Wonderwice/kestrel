#include "ray.h"

// Ray's constructor and at() are defined inline in ray.h so they inline into
// callers (every intersection test constructs rays). Nothing remains here.

namespace kestrel {

}  // namespace kestrel
